import type { ApplicationConfig } from '@angular/core'

export const appConfigServer: ApplicationConfig = {
  // TODO(angular-start-example): add server-only providers here once Angular
  // Start defines SSR rendering, request-scoped DI, and document serialization.
  providers: [],
}
