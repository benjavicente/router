import type { ApplicationConfig } from '@angular/core'

export const appConfig: ApplicationConfig = {
  // TODO(angular-start-example): add shared Angular Start providers here once
  // request context, hydration, transfer state, and router/server composition
  // are defined for the adapter.
  providers: [],
}
