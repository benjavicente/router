# Angular Start Experimental

Scaffold package for Angular Start integration.

This package is intentionally boilerplate-first. Angular-specific SSR and hydration behavior is deferred until the Angular adapter runtime is implemented.
